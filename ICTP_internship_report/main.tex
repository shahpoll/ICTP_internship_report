\documentclass[12pt]{article}

% ===== Packages =====
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{siunitx}
\usepackage{listings}
\usepackage{subcaption}
\usepackage{float}
\usepackage{enumitem}
\usepackage{csquotes}
\usepackage[colorlinks=true,linkcolor=blue,citecolor=teal,urlcolor=purple]{hyperref}
\urlstyle{tt}

\lstset{
  basicstyle=\ttfamily\small,
  frame=single,
  breaklines=true,
  columns=fullflexible
}

\graphicspath{{figs/}}

\title{Reproducible Quantum ESPRESSO Workflows\\Across macOS and Linux for Small-Cell Benchmarks}
\author{Shahriar Pollob\textsuperscript{$\ast$} \quad M.~Shahnoor Rahman\textsuperscript{$\dagger$}\\[0.5em]\small \textsuperscript{$\ast$}Mentee \quad \textsuperscript{$\dagger$}Mentor}
\date{}

\begin{document}

\maketitle
\thispagestyle{empty}

\begin{abstract}
We report a fully reproducible port and validation of Quantum ESPRESSO (v7.4.1) on the Apple M4 architecture under macOS 15 (Sequoia), and benchmark its behaviour against a GPU-accelerated Linux workstation. On the macOS side, we address toolchain regressions introduced by recent Xcode Command Line Tools releases, constructing a hybrid Homebrew/GCC workflow that stabilises preprocessing, BLAS/LAPACK linking, and external library dependencies for CPU-only builds. The resulting binaries are validated against standard silicon electronic-structure and aluminium equation-of-state workflows, recovering PBE-consistent indirect band gaps and an aluminium bulk modulus near 76~GPa. We then execute matched silicon, magnesium oxide, and aluminium EOS workloads on an Ubuntu server (AMD Ryzen 5600G + NVIDIA RTX 4090) using MPI- and CUDA-enabled QE builds. For two-atom primitive cells and a compact nine-point EOS sweep—problem sizes representative of tutorial and classroom exercises—the Mac mini M4 achieves substantially lower wall-clock latency than the server, whose timings are dominated by MPI and CUDA initialisation overheads in this extreme small-system limit. These results do not imply any advantage for Apple Silicon at production scales; rather, they quantify the latency penalty of small workloads on a modern GPU/cluster stack and demonstrate that Apple Silicon workstations can provide a responsive, reproducible platform for educational and pre-production Quantum ESPRESSO workflows. All build scripts, inputs, and provenance artefacts are archived in public repositories to facilitate reuse and extension.
\end{abstract}

\newpage
\thispagestyle{plain}
\vspace*{1em}
\begin{center}
{\Large\bfseries Acknowledgements}
\end{center}

I am grateful to the organisers of the ICTP PWF: Physics for Bangladesh\footnote{\url{https://indico.ictp.it/event/11042}} programme for making this online internship possible and for coordinating the remote collaboration.

\vspace*{1em}
I also thank M.~Shahnoor Rahman\footnote{\url{https://sites.google.com/view/mshahnoorahman}} for providing supervision, access to computing resources, and consistent technical and conceptual feedback throughout this project.

\newpage
\section*{Mentor Approval}
\thispagestyle{plain}
This page is reserved for mentor endorsement of the internship report and the activities documented herein.

\vspace{1.5em}
\noindent\textbf{Mentor:} M.~Shahnoor Rahman\hfill\textbf{Role:} Mentor

\vspace{1.5em}
\noindent\textbf{Statement:} I have reviewed this report and approve it as an accurate record of the work completed during the ICTP PWF: Physics for Bangladesh internship.

\vspace{3em}
\noindent\makebox[0.45\linewidth]{\rule{0.45\linewidth}{0.4pt}}\hfill\makebox[0.3\linewidth]{\rule{0.3\linewidth}{0.4pt}}\\[-0.2em]
\noindent\hspace{1.5em}Signature\hfill Date

\newpage

\tableofcontents
\newpage


\section{Introduction}
Quantum ESPRESSO (QE) is a standard open-source suite for plane-wave density-functional theory and related quantum materials modelling~[1,2]. In typical research workflows, production calculations are executed on Linux clusters or GPU-accelerated workstations, while local laptops or desktops are used for input preparation, convergence studies, and post-processing. The emergence of Apple Silicon (M-series) has made macOS machines increasingly attractive as local development and training platforms, thanks to their high-bandwidth unified memory and strong single-core performance.

However, the recent release of macOS~15 introduced stricter preprocessor and linker defaults that broke existing build recipes for Fortran-based scientific codes, including QE. Guides targeting the original M1 generation were no longer sufficient: the combination of updated Xcode Command Line Tools, reshuffled external libraries (e.g. FoX), and mixed BLAS/LAPACK conventions produced subtle numerical drift or outright build failures. At the same time, it was unclear whether a CPU-only Apple Silicon workflow remained competitive even for the small, tutorial-scale inputs commonly used in classroom or internship settings, compared with a dedicated Linux server equipped with an NVIDIA RTX~4090.

This project therefore had two concrete objectives: (i) to re-establish a stable, reproducible build pipeline for QE~7.4.1 on a Mac mini M4 running macOS~15, and validate the resulting binaries against standard silicon and aluminium test cases; and (ii) to quantify the performance of that pipeline relative to a GPU-accelerated Ubuntu server for small-cell workloads representative of educational training. Section~\ref{sec:m4section} documents the macOS-specific build and validation steps. Sections~\ref{sec:qe-basics} and \ref{sec:qe-stretching-server} describe the ``Path~A'' silicon bands and ``Path~B'' aluminium Birch--Murnaghan equation-of-state workflows on macOS and on the server. Section~\ref{sec:comparison} consolidates the cross-platform benchmarking results, making explicit the distinction between latency-dominated tutorial cases and the large-scale calculations for which GPU-accelerated servers remain essential.

\section{Porting \textsc{Quantum ESPRESSO} to macOS 15 on a Mac~mini M4}
\label{sec:m4section}
At the time we started, only macOS guides aimed at the original M1 generation were available. Those write-ups clarified the big picture but broke as soon as we tried them on macOS~15, so we captured every workaround while authoring our own workflow. This chapter documents that journey of running QE~7.4.1 natively on a Mac~mini M4 under macOS~15 (Sequoia). Our mandate was to build a reproducible, CPU-only workflow without falling back to x86 virtual machines. The accompanying repository\footnote{\url{https://github.com/shahpoll/qe_macos15_build}} archives scripts, logs, and analysis artefacts that mirror the steps reported here.

\subsection{Baseline objectives and constraints}
We began by declaring three non-negotiable outcomes: (i) compile QE with full MPI/OpenMP support using Apple Silicon compilers, (ii) validate the toolchain by reproducing the silicon SCF--NSCF--bands workflow that underpins our convergence studies, and (iii) capture every workaround in automated scripts so a clean macOS~15 install could replay the configuration in one sitting. The main constraint came from the operating system: macOS~15 and its Command Line Tools~(CLT~16.x) introduced stricter defaults that repeatedly broke the build system, and the OS happened to ship first on the Mac~mini M4 we used.

\subsection{Provisioning the native toolchain}
Homebrew served as the package manager of record. Running \verb|brew bundle| against the repository's \texttt{Brewfile} installed \texttt{gcc@14}, \texttt{open-mpi}, \texttt{veclibfort}, \texttt{fftw}, \texttt{libxc}, and Python utilities for plotting. The first stumble surfaced immediately: Apple's system \texttt{cpp} mangled QE's Fortran headers, leading to \verb|laxlib_*.fh| ``no input file'' errors during the \texttt{make pw} phase. Following the notes in \path{docs/Troubleshooting.md}, we pinned the preprocessor explicitly by exporting \verb|CPP="gcc -E"| before running \texttt{configure}. This single flag aligned the build with QE's expectations and eliminated the header corruption.

\subsection{Resolving library and architecture mismatches}
\paragraph{BLAS/LAPACK bindings.} Our first successful build linked against Apple's Accelerate framework through \texttt{veclibfort}, but early tests showed small energy drifts compared with the OpenBLAS reference used on Linux. The autoconf logs revealed that Accelerate exported mixed Fortran symbol names; by overriding \verb|BLAS_LIBS| and \verb|LAPACK_LIBS| to point at \path{/opt/homebrew/opt/lapack} we regained numerical parity. This choice is codified in the helper script \path{scripts/setup/configure_accelerate.sh}.

\paragraph{FoX XML fallout.} QE~7.4.1 decoupled the FoX XML library from its source tree. Our first CMake attempt therefore failed with the ``\texttt{DP\_XML has no implicit type}'' error originating in \texttt{wxml.f90}. The fix was twofold: clone FoX into \texttt{external/fox} and remove the \texttt{\_\_XML\_STANDALONE} define injected by CMake. Automating that change inside \path{scripts/setup/patch_fox.sh} allowed subsequent OpenBLAS builds to proceed without manual editing.

\paragraph{Architecture flags.} Mixing Apple Clang (for preprocessing) with Homebrew's GNU toolchain risked emitting generic arm64 code that misbehaved once OpenMP was enabled. We forced native tuning by exporting
\begin{lstlisting}[frame=single]
FC="gfortran -mcpu=apple-m4"
MPIF90="mpif90 -mcpu=apple-m4"
\end{lstlisting}
which ensured every object file targeted the M4 microarchitecture. The resulting binaries passed \texttt{otool -L} inspections recorded under \path{logs/linker/}.

\subsection{Automating builds and reproducibility}
With the core hurdles addressed, we wrapped the workflow in reproducible artefacts. The helper script \path{scripts/setup/fetch_qe.sh} fetches tagged QE releases and stages them under \path{artifacts/}, while \path{scripts/setup/configure_accelerate.sh} replays the successful \texttt{configure} flags, including the corrected BLAS/LAPACK paths and preprocessor override. \url{CMakePresets.json} captures the experimental OpenBLAS flow so future iterations can switch linear algebra back-ends with a single preset, and the smoke test at \path{scripts/smoke_test.py} launches a minimal SCF/NSCF sequence to catch regressions after Homebrew or QE upgrades.
Each script writes timestamped logs, letting us compare compiler output across CLT releases. This logging proved essential when Apple issued stealth updates to the toolchain mid-internship.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
    node distance=9mm and 26mm,
    process/.style={rectangle, rounded corners, draw=blue!60, fill=blue!10, very thick, minimum width=36mm, align=center, inner ysep=6pt},
    arrow/.style={-Latex, thick}
  ]
    \node[process] (m1) {Review M1-era guides\\Identify incompatible steps};
    \node[process, below=of m1] (toolchain) {Provision Homebrew toolchain\\\small(\texttt{brew bundle})};
    \node[process, below=of toolchain] (patches) {Apply macOS~15 fixes\\\small(CPP override, BLAS relink, FoX patch)};
    \node[process, below=of patches] (build) {Compile QE with arm64 flags};
    \node[process, below=of build] (tests) {Run smoke tests\\Archive logs and scripts};
    \draw[arrow] (m1) -- (toolchain);
    \draw[arrow] (toolchain) -- (patches);
    \draw[arrow] (patches) -- (build);
    \draw[arrow] (build) -- (tests);
  \end{tikzpicture}
  \caption{High-level workflow for porting QE to macOS~15 on the Mac~mini M4.}
  \label{fig:qe-flow}
\end{figure}

\subsection{Validation runs and scientific checkpoints}
We validated the resulting binaries using the silicon case housed in \path{cases/si/manual}. The SCF baseline converged in five iterations with a Fermi energy of 6.4346~eV, matching the reference values stored in \path{cases/si/comparison/data/fermi_consistency.csv}. Subsequent NSCF, DOS, and projected DOS steps reproduced the 0.5699~eV indirect gap within 1~meV of the Linux reference calculation. Benchmark timings showed the canonical silicon SCF input completing in $39\pm1$~s on two MPI ranks (one OpenMP thread each), consistent with the \path{THREAD7_REPORT.md} snapshots archived in the repository. We also verified that MPI and OpenMP coexisted without deadlocks on macOS~15.2 by replaying the PWTK-driven automation under \path{cases/si/pwtk/}, which stresses rank/thread affinity differently from the manual run. These silicon checks form the core of the ``Path~A'' workflow later used in the stretching exercises. The legacy M1 guides helped us shape these validation steps even though we had to refit every command for the newer OS.

\subsection{Residual issues and safeguards}
Two limitations remain. First, OpenBLAS builds via CMake still require manual FoX patching; we documented the procedure thoroughly but continue to prefer the autoconf-based path that links QE against the Homebrew LAPACK stack described in Section~\ref{sec:m4section} for day-to-day work. Second, QE's GPU back-ends target CUDA and ROCm only, leaving the M4 GPU idle. Our mitigation strategy is to keep CPU workflows efficient and document the gap in \path{docs/AppleSilicon_QE_Guide.md}. To guard against regressions when Apple or Homebrew ship new compilers, we pinned key formulae versions in the \texttt{Brewfile} and enabled GitHub dependabot alerts on the repository.

\subsection{Summary and concluding remarks}
Porting QE to macOS~15 on the Mac~mini M4 demanded more attention to the build ecosystem than to the physics kernels themselves. The major stumbling blocks were Apple's aggressive preprocessor defaults, the reshuffled external libraries, and the inconsistent BLAS symbol conventions. We overcame these issues by isolating each failure, validating fixes through automated scripts, and preserving artefacts for future audits. The resulting workflow delivers reproducible SCF, band-structure, and DOS analyses on native hardware without requiring x86 emulation or virtualisation. Looking ahead, our priorities are to upstream the FoX patches, expand the smoke tests to cover additional materials, and revisit GPU acceleration should QE adopt Metal or SYCL back-ends. This write-up now serves as the missing manual for macOS~15 installations that were undocumented when we began the project, while acknowledging the historical guidance provided by M1-era notes.

\section{\textsc{QE\_stretching} Workflows on macOS (Paths~A and B)}
\label{sec:qe-basics}
With the toolchain validated, we refreshed the ``stretching'' exercises using the updated Mac workflows hosted in the QE\_stretching\_macm4 repository\footnote{\url{https://github.com/shahpoll/QE_stretching_macm4}}. This report focuses on two of the three paths in that project: Path~A, a silicon electronic-structure workflow (SCF $\rightarrow$ NSCF $\rightarrow$ bands, DOS, and PDOS), and Path~B, an aluminium equation-of-state sweep fitted to a third-order Birch--Murnaghan form. Path~C, which targets more complex magnetic systems, remains experimental and is not discussed here. Both paths run under the same \textsc{pw.x} build described earlier and retain provenance captures (environment, pseudopotential checksums, timing probes) under each path's \texttt{work/} directory.

\subsection{Path A — Silicon bands, DOS, and PDOS}
The refreshed Path~A workflow (\path{pathA\_si\_mac}) follows the standard QE sequence: SCF on an $8\times8\times8$ mesh, NSCF on a $12\times12\times12$ mesh, and band tracing along the FCC high-symmetry path, followed by \texttt{bands.x}, \texttt{dos.x}, and \texttt{projwfc.x}. Smearing is removed for SCF; tetrahedra are used for NSCF to sharpen the indirect gap. Full run scripts are provided in Appendix~\ref{app:repro}. Performance on eight ranks completes SCF in $2.4$~s, bands in $30.3$~s, NSCF in $8.2$~s, and DOS/PDOS in $\sim6.7$~s. The indirect gap from \texttt{si.bands.dat.gnu} is $0.56$~eV, consistent with PBE expectations and the benchmarking suite~[4]. Provenance (environment, pseudopotential checksums, timing probes) remains in \texttt{work/}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.86\linewidth]{si_bands_mac.png}
  \caption{Silicon band structure from Path~A on macOS. Horizontal dashed line marks the Fermi level; HO/LU annotations and the indirect gap are derived directly from the parsed \texttt{si.bands.dat.gnu} file.}
  \label{fig:pathA-bands}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.7\linewidth]{si_dos_mac.png}
  \caption{Total density of states for silicon (Path~A) using a $12\times12\times12$ NSCF mesh and Gaussian broadening $\sigma=0.05$~eV. The shaded region highlights states above the Fermi level; QE-only and QE~vs~Materials Project overlays are regenerated by \texttt{scripts/plot\_dos.py}.}
  \label{fig:pathA-dos}
\end{figure}

\subsection{Path B — Aluminium equation of state}
Path~B (\path{pathB_eos_al_mac}) now packages a full Birch--Murnaghan third-order fit pipeline with LaTeX-ready artefacts. Fifteen SCF calculations span lattice scalings from $0.90$ to $1.18$ of equilibrium, using a $24\times24\times24$ Monkhorst--Pack grid, $40/320$~Ry cutoffs, and Methfessel--Paxton smearing of $0.02$~Ry to stabilise the metallic occupations during the sweep. The PAW pseudopotential \texttt{Al.pbe-n-kjpaw\_psl.1.0.0.UPF} is tracked in \texttt{work/pp\_checksums.txt}. Running
the provided harvest/fit scripts (Appendix~\ref{app:repro}) rebuilds all tables and figures from the raw QE outputs in \texttt{work/}.

  The BM3 fit converges to $a_0 = 4.0373$~\AA, $B_0 = 76.20$~GPa, $B^\prime = 4.830$ with an RMS of $1.48$~meV/atom (values mirrored in \texttt{work/eos\_fit.json} and \texttt{latex/eos\_table.tex}). Figures~\ref{fig:pathB-eos} and \ref{fig:pathB-residuals} show the energy--volume curve and residuals; all deviations lie within $\pm2$~meV/atom, negligible compared with the binding energy, confirming that a third-order Birch--Murnaghan form is adequate near equilibrium~[3,5].

\begin{figure}[H]
  \centering
  \includegraphics[width=0.72\linewidth]{eos_E_vs_V_pub.png}
  \caption{Energy--volume curve for fcc aluminium (Path~B). Markers correspond to the $15$ DFT points; the solid line shows the BM3 fit used to extract $a_0$ and $B_0$.}
  \label{fig:pathB-eos}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.72\linewidth]{eos_residuals_pub.png}
  \caption{Residual energies ($E_\text{DFT}-E_\text{BM3}$) for Path~B. All deviations remain within $\pm2$~meV/atom, supporting the reported fit quality.}
  \label{fig:pathB-residuals}
\end{figure}

\subsection{Cross-path observations}
The updated exercises confirm the macOS~15 stack is both fast and scientifically faithful. Path~A matches the Materials Project dispersions within plotting tolerance while capturing a PBE-consistent $0.56$~eV gap, and its 8-rank timing profile delivers sub-minute end-to-end turnaround. Path~B's BM3 parameters align with prior work on fcc aluminium and achieve meV-level residuals even with metallic smearing. Both paths reuse the same QE binaries, pseudopotential hygiene, and Python analysis scripts, strengthening confidence that later comparisons against the server (Section~\ref{sec:comparison}) reflect platform differences rather than workflow drift.

\section{Stretching Server Environment}
\label{sec:server}
The reference environment consisted of an Ubuntu~24.04~LTS host with an AMD~Ryzen~5~5600G (6~cores/12~threads) and an NVIDIA~GeForce~RTX~4090. Quantum~ESPRESSO~7.4.1 was compiled against the NVIDIA~HPC~SDK~25.3 toolchain using CUDA-aware OpenMPI~4.1 (UCX transport). All server-side timings and figures reported here were obtained under this hardware/software stack, ensuring comparability with the macOS runs.

\begin{table}[H]
  \centering
  \begin{tabularx}{\linewidth}{@{}lXX@{}}
    \toprule
    Component & Mac mini M4 & Ubuntu Server \\
    \midrule
    CPU Model & Apple M4 (10 cores) & AMD Ryzen 5 5600G (6C/12T) \\
    GPU Model & Integrated Apple GPU (unused) & NVIDIA GeForce RTX 4090 \\
    RAM & 16~GB unified & 32~GB DDR4 + 24~GB GDDR6X \\
    OS & macOS 15.3 & Ubuntu 24.04.3 LTS \\
    MPI Version & OpenMPI~5.x (Homebrew) & CUDA-aware OpenMPI~4.1 (HPC SDK~25.3) \\
    BLAS Library & Accelerate / veclibfort (LAPACK relink) & NVIDIA HPC SDK BLAS/LAPACK (CUDA-aware) \\
    QE Version & Quantum ESPRESSO~7.4.1 & Quantum ESPRESSO~7.4.1 \\
    \bottomrule
  \end{tabularx}
  \caption{Hardware \& software specification comparing the macOS and Ubuntu environments used in this study.}
  \label{tab:env-compare}
\end{table}

\section{\textsc{QE\_stretching\_server} Workflows (Paths~A and B)}
\label{sec:qe-stretching-server}
To validate the remote environment we replayed the Path~A and Path~B exercises using the repository at \href{https://github.com/shahpoll/QE_stretching_server}{\texttt{QE\_stretching\_server}}. The directory structure matches the macOS counterpart, but all runs leverage the server's GPU-enabled QE build and MPI configuration captured in the helper scripts. Full launch scripts and command listings are provided in Appendix~\ref{app:repro}.

\subsection{Path A — Silicon on the server}
The silicon workflow resides in the \texttt{pathA\_si} subdirectory of the QE\_stretching\_server repository on the Ubuntu host. Runs use six MPI ranks (\texttt{-np 6}, \texttt{OMP\_NUM\_THREADS=1}) against the NVIDIA HPC SDK 25.3 QE~7.4.1 build. Representative timings from \texttt{work/runlog.txt}: SCF $15.6$~s (2 iterations, HO~6.208~eV), bands $4$~min~$14$~s, NSCF $1$~min~$47$~s (HO~6.208~eV, LU~6.790~eV), DOS $0.9$~s, PDOS $4.8$~s. The extracted indirect gap of $0.582$~eV matches the macOS calculation within rounding. Convergence scans documented in \texttt{work/conv\_cutoff.csv} and \texttt{work/conv\_kmesh.csv} show total energies stabilising to the meV level by $36$~Ry and $12\times12\times12$ sampling, while the final plots (\texttt{plots/si\_bands.png}, \texttt{plots/si\_dos.png}) reflect the GPU-enabled post-processing pipeline.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.84\linewidth]{server_si_bands.png}
  \caption{Silicon band structure from the stretching server (Path~A), calculated on Ubuntu/RTX~4090 to verify numerical consistency with macOS results. Values printed on the plot stem from the GPU-enabled QE run captured in \href{https://github.com/shahpoll/QE_stretching_server/blob/main/pathA_si/plots/si_bands_labeled.png}{\texttt{plots/si\_bands\_labeled.png}}.}
  \label{fig:server-pathA-bands}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.65\linewidth]{si_dos_smooth.png}
  \caption{Total DOS for silicon (Path~A) generated on Ubuntu/RTX~4090 to verify numerical consistency with macOS results. The smoothed profile reproduces the macOS calculation while recording GPU timings in \texttt{work/runlog.txt}.}
  \label{fig:server-pathA-dos}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.82\linewidth]{si_bands_dos_combined.png}
  \caption{Combined bands and DOS view from the server computation (Path~A, Ubuntu/RTX~4090), aligning the indirect gap seen in the dispersion with the DOS minimum. Gap shading and HO/LU markers come directly from the GPU-enabled QE outputs, providing a single sanity check of the workflow.}
  \label{fig:server-pathA-bands-dos}
\end{figure}

\subsection{Path B — Aluminium EOS on the server}
The aluminium equation-of-state workflow resides in the \texttt{pathB\_eos\_al} subdirectory of QE\_stretching\_server. It packages a resume-safe 15-point sweep (0.90--1.18~$a_0$) with a $24\times24\times24$ mesh, $40/320$~Ry cutoffs, and Marzari--Vanderbilt (MV) cold smearing $\sigma=0.02$~Ry, driven by the GPU-enabled QE~7.4.1 build. The NVIDIA HPC SDK/HPC-X environment is loaded inside \texttt{scripts/run\_eos.sh} before launching \texttt{mpirun -np 6} with \texttt{OMP\_NUM\_THREADS=1}; completed scales are skipped automatically.
Harvesting and fitting via \texttt{scripts/harvest.sh} and \texttt{scripts/plot\_final.gp} regenerate \texttt{work/eos\_summary.json}, LaTeX snippets in \texttt{latex/}, and publication-ready plots in \texttt{plots/}. The BM3 fit yields $a_0 = 4.0373 \pm 0.0007$~\AA, $B_0 = 76.018 \pm 0.254$~GPa, $B^\prime = 4.824 \pm 0.028$, with RMS residual $1.269$~meV/atom and max$| \Delta E|=1.879$~meV/atom, consistent with the macOS fit within uncertainties. Full run scripts are provided in Appendix~\ref{app:repro}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.72\linewidth]{eos_E_vs_V_pub_py.png}
  \caption{Energy--volume curve for fcc aluminium (Path~B) calculated on Ubuntu/RTX~4090 to verify numerical consistency with macOS results. Markers denote the $15$ MPIRUN samples; the solid line is the BM3 fit saved in \texttt{work/eos\_summary.json}.}
  \label{fig:server-pathB-eos}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.72\linewidth]{eos_residuals_pub_py.png}
  \caption{Residual energies for the aluminium EOS fit calculated on Ubuntu/RTX~4090 to verify numerical consistency with macOS results. The maximum absolute deviation stays below $2$~meV/atom, matching the statistics embedded in \texttt{latex/eos\_table.tex}.}
  \label{fig:server-pathB-residuals}
\end{figure}

The residuals remain within $\pm2$~meV/atom, negligible relative to the binding energy, supporting the adequacy of the third-order Birch--Murnaghan description of the lattice near equilibrium in the server run.

\section{Mac mini vs Server Performance Comparison}
\label{sec:comparison}
We reran the head-to-head measurements using the consolidated repository\footnote{\url{https://github.com/shahpoll/qe_benchmarking}} (\texttt{qe\_benchmarking}). Each run records the launcher (\texttt{command.log}), environment snapshot (\texttt{env.log}), hardware inventory (\texttt{hardware.log}), QE output (\texttt{pw.out}), and timing probe (\texttt{time.log}). Aggregated wall, CPU, and core-second totals live in \texttt{runs/local\_summary.tsv}, with slowdown ratios summarised in \texttt{runs/comparison\_summary.txt}. The inclusion of a bulk MgO workload and a multi-volume aluminium sweep reduces the risk of timings being dominated purely by MPI or CUDA start-up, addressing the concerns raised after the initial tutorial-sized benchmarks. The matrix of workloads is visualised in Figure~\ref{fig:benchmark-matrix}. All benchmark timings reported here are single-shot wall-clock measurements on otherwise idle machines; they characterise representative latency for the chosen configurations rather than fully tuned performance limits.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\linewidth]{workloads_overview.png}
  \caption{Benchmark suite tracked in \texttt{qe\_benchmarking}: bulk silicon, bulk magnesium oxide, and a nine-point aluminium EOS sweep, each executed on the Mac (1- and 8-rank CPU) and the server (6-rank CPU and 4-rank GPU builds). These rank counts were chosen as simple, reasonable configurations and were not exhaustively tuned for peak performance.}
  \label{fig:benchmark-matrix}
\end{figure}

\subsection{Physics sanity checks}
Before reading timings, we validated that the electronic-structure outputs remain physically sensible. The silicon bulk run yields a PBE gap of $0.560$~eV (Figure~\ref{fig:si-band-dos}), matching the $0.5597$~eV reported in \texttt{runs/si\_bulk\_bands/summary.txt} and consistent with Materials Project references~[4]. Magnesium oxide returns a $4.74$~eV gap (Figure~\ref{fig:mgo-band-dos}), consistent with the usual PBE underestimate relative to experiment~[4]. For all three workloads, the pseudopotential filenames and SHA256 hashes are recorded in the workflow repositories (\texttt{pp\_checksums.txt} under the corresponding \texttt{runs/*} directories), so the exact potentials can be recovered and re-used. The aluminium EOS fit (Figures~\ref{fig:al-eos-fit-curve} and \ref{fig:al-eos-per-volume}) gives $B_0 \approx 26$~GPa and $B_0' \approx 6.35$ per \texttt{runs/al\_eos\_fit/fit\_summary.txt}. This bulk modulus is unphysically soft compared to the $\approx 76$~GPa obtained in the converged Path~B workflow and to standard experimental values. Here the discrepancy is intentional: the benchmarking input uses deliberately broad Marzari--Vanderbilt (MV) cold smearing ($0.02$~Ry) and a coarse sampling strategy to stabilise timing and stress the launch overheads, not to recover accurate elastic properties. The physically meaningful aluminium EOS is the converged Path~B sweep in Sections~\ref{sec:qe-basics} and \ref{sec:qe-stretching-server}, which yields $B_0 \approx 76$~GPa on both platforms.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.82\linewidth]{si_bulk_bands_dos.png}
  \caption{Silicon bulk band structure and DOS from \texttt{qe\_benchmarking}; shaded regions and labels originate from \texttt{runs/si\_bulk\_bands/summary.txt}.}
  \label{fig:si-band-dos}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.82\linewidth]{mgo_bulk_bands_dos.png}
  \caption{Magnesium oxide band structure and DOS from \texttt{qe\_benchmarking}; the PBE gap of $4.74$~eV is consistent with \texttt{runs/mgo\_bulk\_bands/summary.txt}.}
  \label{fig:mgo-band-dos}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.8\linewidth]{al_eos_fit.png}
  \caption{Birch--Murnaghan fit parameters from the benchmarking sweep in \texttt{runs/al\_eos\_fit}.}
  \label{fig:al-eos-fit-curve}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.8\linewidth]{al_eos_per_volume.png}
  \caption{Per-atom energies versus lattice scaling for the aluminium EOS benchmark.}
  \label{fig:al-eos-per-volume}
\end{figure}

\subsection{Timing outcomes: latency versus throughput}
Across all three workloads the Mac mini retains a clear lead. From \texttt{runs/comparison\_summary.txt}, the server's CPU build (6 ranks) is $15$--$17\times$ slower than the Mac's 8-rank run, while the GPU-enabled build (4 ranks) still lags by $10$--$12\times$. Figures~\ref{fig:timing-si}--\ref{fig:timing-al} visualise these penalties; \texttt{runs/local\_summary.tsv} lists per-input wall, CPU, and SCF-iteration counts. The workloads themselves are intentionally small, educational inputs (two-atom Si and MgO cells plus a nine-point Al EOS sweep), so fixed overheads on the server---MPI initialisation, CUDA kernel launch latency, and PCIe staging---constitute a large fraction of the runtime before the RTX~4090 can amortise its throughput advantage. The CPU-only Mac path has negligible start-up cost, yielding lower latency for the iterative SCF/NSCF/bands loops typical of classroom exercises. These measurements should therefore be interpreted strictly as latency benchmarks in the small-cell limit; they do not imply that the Mac mini M4 can outperform a GPU-accelerated server for larger supercells, denser k-point meshes, or production-scale workloads where the RTX~4090 can amortise its start-up costs.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.72\linewidth]{si_bulk_mac_vs_server.png}
  \caption{Silicon bulk wall-clock timings on the Mac (1- and 8-rank CPU) versus the server (6-rank CPU and 4-rank GPU). Slowdown factors: $17.1\times$ (CPU) and $10.4\times$ (GPU).}
  \label{fig:timing-si}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.72\linewidth]{mgo_bulk_mac_vs_server.png}
  \caption{MgO bulk wall-clock timings across the same configurations. Slowdown factors: $17.2\times$ (CPU) and $11.8\times$ (GPU).}
  \label{fig:timing-mgo}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.72\linewidth]{al_eos_mac_vs_server.png}
  \caption{Aluminium EOS wall-clock timings. Slowdown factors: $17.4\times$ (CPU) and $12.3\times$ (GPU).}
  \label{fig:timing-al}
\end{figure}

Absolute timings reinforce the ratios: the Mac finishes the silicon bulk SCF--bands workflow in $19$~s on eight ranks versus $332$~s on the server CPU and $203$~s on the server GPU; MgO requires $27$~s versus $459$~s and $316$~s; individual aluminium EOS points take $6$--$10$~s on the Mac and $103$--$153$~s on the server, depending on the build. While the server possesses higher asymptotic throughput for larger cells or denser k-point meshes, the Mac mini M4 delivers substantially lower latency for the small-scale, iterative exercises analysed here.

\subsection{Summary of findings}
These measurements characterise workflow latency in the extreme small-problem regime rather than asymptotic throughput. For the two-atom unit cells and compact EOS sweep considered here, the server's GPU kernel launch latency and MPI start-up dominate wall time and largely mask the raw compute advantage of the RTX~4090. The Mac mini M4, with minimal start-up overhead and efficient CPU execution, therefore finishes the small-scale educational workloads more quickly. This outcome is expected to invert for substantially larger supercells or denser k-point meshes, where the server's higher peak throughput and GPU acceleration can be amortised over many SCF cycles; in that regime the Mac mini M4 will not compete with a dedicated RTX~4090 node. In this report, the benchmarks are intentionally restricted to tutorial-scale cases to expose the penalty of small-system overhead.

\subsection{Environment notes}
The server logs retain signs of stack overhead: \texttt{time.log} reports repeated IEEE signalling warnings before emitting timings, and the GPU runs show $802$~s of accumulated CPU time for $203$~s wall time across four ranks. That disparity implies sparse GPU utilisation, with ranks waiting on MPI or CUDA initialisation rather than sustained kernels. Launcher settings in \texttt{command.log} and \texttt{env.log} (\texttt{--map-by ppr:1:core --mca btl self,vader --mca pml ucx}, \texttt{CUDA\_VISIBLE\_DEVICES=0}, \texttt{OMP\_NUM\_THREADS=1}) document the UCX/HPC-X MPI stack that likely dominates start-up and communication costs. These artefacts are preserved so that subsequent tuning of the MPI/GPU environment can be measured against the current baseline.

\section{Conclusion}
This internship established a fully reproducible Quantum ESPRESSO environment on macOS 15, overcoming toolchain instability on the Apple M4 platform. By disentangling preprocessor behaviour, BLAS/LAPACK linking, and external library dependencies, we produced a CPU-only build pipeline that can be replayed on fresh Apple Silicon hardware using automated scripts. Validation against silicon and aluminium test cases showed that the macOS binaries recover PBE-consistent indirect gaps and an aluminium bulk modulus near 76~GPa, matching a GPU-accelerated Ubuntu server within meV and GPa tolerances.

The comparative benchmarks highlight a clear architectural trade-off. For very small, tutorial-scale workloads---two-atom silicon and magnesium oxide primitive cells, and a compact aluminium EOS sweep—the Mac mini M4 delivers significantly lower wall-clock latency than an RTX 4090-equipped server, whose timings are dominated by MPI and CUDA start-up overheads in this regime. These results should not be extrapolated to production-scale calculations: for larger supercells, denser k-point meshes, or more demanding correlated systems, the higher asymptotic throughput of GPU-accelerated servers will remain essential.

Within that limited scope, however, Apple Silicon workstations provide a responsive, scientifically faithful platform for teaching, pre-production convergence studies, and post-processing. Future work will extend these pipelines to more complex magnetic systems (including Path~C), integrate additional automated smoke tests and regression checks, and explore potential routes to GPU acceleration on macOS, such as prospective Metal- or SYCL-based back-ends in Quantum ESPRESSO.

\section{References}
\begin{enumerate}[label={[}\arabic*{]}]
  \item P.~Giannozzi \textit{et al.}, ``QUANTUM ESPRESSO: a modular and open-source software project for quantum simulations of materials,'' \textit{J. Phys.: Condens. Matter} \textbf{21}, 395502 (2009).
  \item P.~Giannozzi \textit{et al.}, ``Advanced capabilities for materials modelling with Quantum ESPRESSO,'' \textit{J. Chem. Phys.} \textbf{152}, 154105 (2020).
  \item F.~Birch, ``Finite elastic strain of cubic crystals,'' \textit{Phys. Rev.} \textbf{71}, 809--824 (1947).
  \item K.~A. Persson, ``Materials data on Si (silicon) and MgO (magnesium oxide),'' Materials Project (\url{https://materialsproject.org}), accessed 2025.
  \item N.~W. Ashcroft and N.~D. Mermin, \textit{Solid State Physics}, Holt, Rinehart and Winston (1976).
\end{enumerate}

\newpage

\appendix

\section{Reproduction Scripts}
\label{app:repro}
Complete shell scripts, launcher commands, and file listings for all workflows are provided in the accompanying repositories and are indexed here for reproducibility. This includes the Path~A SCF$\rightarrow$NSCF$\rightarrow$ \newline bands$\rightarrow$DOS/PDOS pipelines on macOS and server, and the Path~B aluminium EOS sweep scripts. Refer to the repository paths noted in the main text for the latest versions.

\section{Supplementary Materials}
\label{app:code}
The following resources accompany this report and are available in the referenced repositories:
\begin{sloppypar}
\begin{itemize}[leftmargin=*]
  \setlength\itemsep{4pt}
  \item \textbf{Build and run scripts}: configure\_accelerate.sh, fetch\_qe.sh, and the Path~A / Path~B launchers in QE\_stretching\_macm4\\(pathA\_si\_mac/scripts and pathB\_eos\_al\_mac/scripts).
  \item \textbf{Provenance bundles}: environment snapshots, pseudopotential hashes, and command logs stored in each workflow's work/ directory (local and server). On the Ubuntu host, QE\_stretching\_server was cloned under \path{/home/pollob/qe_basics} with subdirectories \texttt{pathA\_si} and \texttt{pathB\_eos\_al}.
  \item \textbf{Comparison data and figures}: logs, timing tables, and plots in qe\_benchmarking\\(runs/local\_summary.tsv, runs/comparison\_summary.txt, runs/plots/*.png); the repository was staged under \path{/home/pollob/qe_benchmarking} on the server during the study.
  \item \textbf{LaTeX-ready artefacts}: eos\_table.tex, eos\_methods.tex, and runs/al\_eos\_fit/fit\_summary.tex in qe\_benchmarking.
  \item \textbf{Raw figures referenced here}: PNG files under figs/ of this repository, copied directly from the workflow and benchmarking projects.
\end{itemize}
\end{sloppypar}

\section*{Repository Index}
\begin{itemize}
  \item \href{https://github.com/shahpoll/qe_macos15_build}{\texttt{qe\_macos15\_build}} — scripts and documentation for building QE~7.4.1 on macOS~15; typically cloned under a \texttt{qe\_macos15\_build} directory on the macOS host.
  \item \href{https://github.com/shahpoll/QE_stretching_macm4}{\texttt{QE\_stretching\_macm4}} — Mac mini Path~A and Path~B workflows with logs, plots, and provenance; typically cloned under a \texttt{QE\_stretching\_macm4} directory on macOS.
  \item \href{https://github.com/shahpoll/QE_stretching_server}{\texttt{QE\_stretching\_server}} — Stretching server equivalents of the Path~A and Path~B workflows; cloned on the Ubuntu host under a \texttt{QE\_stretching\_server} directory (e.g., under \texttt{qe\_basics}).
  \item \href{https://github.com/shahpoll/qe_benchmarking}{\texttt{qe\_benchmarking}} — Consolidated benchmarking suite (logs, timings, band/DOS checks, and plots) for the Mac-versus-server study; staged on the server under a \texttt{qe\_benchmarking} directory.
\end{itemize}

\end{document}
